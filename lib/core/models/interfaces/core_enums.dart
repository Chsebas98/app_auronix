enum TypeToast { defaultToast, actionToast }

enum TypeColor { success, error, warning }

enum TypeElementToast {
  toastBg,
  toastButton,
  toastText,
  toastBorder,
  toastShadow,
}

enum TypeDialog { resDialog, actionDialog, optionDialog }

enum Roles { rolAdmin, rolGerente, rolDriver, rolMember, rolUser }

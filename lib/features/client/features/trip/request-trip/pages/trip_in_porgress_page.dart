import 'package:flutter/material.dart';

class TripInPorgressPage extends StatelessWidget {
  const TripInPorgressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      appBar: null,
      body: Text('TripInPorgressPage'),
    );
  }
}

import 'package:flutter/material.dart';

class ClientTripTemplate extends StatelessWidget {
  const ClientTripTemplate({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('ClientTripTemplate'));
  }
}

import 'package:flutter/material.dart';

class CustomInputSelect extends StatelessWidget {
  const CustomInputSelect({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('CustomInputSelect'));
  }
}
